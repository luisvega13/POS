import {KeyRound,Plus,Save,ShieldCheck,SlidersHorizontal,UserRoundCheck,UsersRound} from 'lucide-react';
import {FormEvent,useEffect,useState} from 'react';
import {api} from '../api';
import {Modal} from '../components/Modal';
import type {OperationMode,Permissions,User,UserRole} from '../types';

const roles:{id:UserRole;name:string;description:string}[]=[{id:'admin',name:'Administrador',description:'Control total, configuración, productos y usuarios.'},{id:'cashier',name:'Caja',description:'Ventas, cobro de mesas, reportes y corte de caja.'},{id:'waiter',name:'Mesero',description:'Apertura de mesas, pedidos y consulta de consumos.'}];
export function UsersPage({permissions,onPermissions,onNotice}:{permissions:Permissions;onPermissions:(permissions:Permissions)=>void;onNotice:(message:string,error?:boolean)=>void}){
 const [users,setUsers]=useState<User[]>([]),[editing,setEditing]=useState<User|null|undefined>(undefined),[name,setName]=useState(''),[username,setUsername]=useState(''),[password,setPassword]=useState(''),[role,setRole]=useState<UserRole>('waiter'),[saving,setSaving]=useState(false);
 const [policy,setPolicy]=useState(permissions);useEffect(()=>setPolicy(permissions),[permissions]);
 const load=()=>api.users().then(setUsers).catch(error=>onNotice(error.message,true));useEffect(()=>{void load()},[]);
 function open(user:User|null){setEditing(user);setName(user?.display_name??'');setUsername(user?.username??'');setPassword('');setRole(user?.role??'waiter')}
 async function save(event:FormEvent){event.preventDefault();setSaving(true);try{if(editing)await api.updateUser(editing.id,{display_name:name,role,...(password?{password}:{})});else await api.createUser({display_name:name,username,password,role});setEditing(undefined);await load();onNotice(editing?'Usuario actualizado':'Usuario creado')}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 async function toggle(user:User){try{await api.updateUser(user.id,{active:!user.active});await load();onNotice(user.active?'Usuario desactivado':'Usuario activado')}catch(error){onNotice((error as Error).message,true)}}
 async function savePolicy(){setSaving(true);try{const saved=await api.savePermissions(policy);setPolicy(saved);onPermissions(saved);onNotice('Permisos actualizados')}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 const modeField=(label:string,key:'waiter_transfer_mode'|'cashier_transfer_mode'|'waiter_cancel_mode'|'cashier_cancel_mode')=><label className="field">{label}<select value={policy[key]} onChange={event=>setPolicy(current=>({...current,[key]:event.target.value as OperationMode}))}><option value="allowed">Permitido</option><option value="password">Con contraseña de administrador</option><option value="denied">Bloqueado</option></select></label>;
 return <><div className="section-title"><div><h2>Usuarios y perfiles</h2><p>Asigna a cada persona únicamente las funciones que necesita.</p></div><button className="primary" onClick={()=>open(null)}><Plus size={17}/> Nuevo usuario</button></div><section className="panel permissions-panel"><header><SlidersHorizontal size={19}/><div><h3>Permisos operativos</h3><p>Define qué puede hacer cada perfil y qué operaciones requieren autorización.</p></div></header><div className="permission-grid"><label className="check-field"><input type="checkbox" checked={policy.print_on_checkout} onChange={event=>setPolicy(current=>({...current,print_on_checkout:event.target.checked}))}/><span>Imprimir ticket al cobrar<small>Si se desactiva, el cobro solo queda registrado en el sistema.</small></span></label><label className="check-field"><input type="checkbox" checked={policy.waiter_require_guest_count} onChange={event=>setPolicy(current=>({...current,waiter_require_guest_count:event.target.checked}))}/><span>Solicitar número de personas<small>El mesero deberá capturarlo al abrir la mesa.</small></span></label><label className="check-field"><input type="checkbox" checked={policy.cashier_table_access} onChange={event=>setPolicy(current=>({...current,cashier_table_access:event.target.checked}))}/><span>Caja puede acceder a Mesas<small>Desactívalo para limitar Caja al punto de venta.</small></span></label><label className="check-field"><input type="checkbox" checked={policy.waiter_print_account} onChange={event=>setPolicy(current=>({...current,waiter_print_account:event.target.checked}))}/><span>Mesero puede imprimir la cuenta<small>La impresión no cobra ni cierra la mesa.</small></span></label>{modeField("Traspasos · Mesero","waiter_transfer_mode")}{modeField("Traspasos · Caja","cashier_transfer_mode")}{modeField("Cancelaciones · Mesero","waiter_cancel_mode")}{modeField("Cancelaciones · Caja","cashier_cancel_mode")}<label className="check-field"><input type="checkbox" checked={policy.include_tip_in_ticket} onChange={event=>setPolicy(current=>({...current,include_tip_in_ticket:event.target.checked,include_suggested_tip:event.target.checked?false:current.include_suggested_tip}))}/><span>Incluir cargo de servicio obligatorio<small>Calcula el porcentaje, lo suma a la cuenta y lo registra como propina.</small></span></label>{policy.include_tip_in_ticket&&<label className="field">Porcentaje de servicio<input type="number" min="0" max="100" value={policy.service_charge_percent} onChange={event=>setPolicy(current=>({...current,service_charge_percent:Number(event.target.value)}))}/></label>}<label className="check-field"><input type="checkbox" checked={policy.include_suggested_tip} onChange={event=>setPolicy(current=>({...current,include_suggested_tip:event.target.checked,include_tip_in_ticket:event.target.checked?false:current.include_tip_in_ticket}))}/><span>Mostrar propina sugerida en la cuenta<small>Solo informa el porcentaje; no lo suma al total.</small></span></label>{policy.include_suggested_tip&&<label className="field">Porcentaje sugerido<input type="number" min="0" max="100" value={policy.suggested_tip_percent} onChange={event=>setPolicy(current=>({...current,suggested_tip_percent:Number(event.target.value)}))}/></label>}</div><footer><button className="primary" disabled={saving} onClick={()=>void savePolicy()}><Save size={16}/>{saving?"Guardando…":"Guardar permisos"}</button></footer></section><div className="role-overview">{roles.map(item=><article className="panel" key={item.id}>{item.id==='admin'?<ShieldCheck/>:item.id==='cashier'?<UserRoundCheck/>:<UsersRound/>}<div><b>{item.name}</b><p>{item.description}</p></div></article>)}</div><section className="panel data-panel"><div className="table-wrap"><table><thead><tr><th>Nombre</th><th>Usuario</th><th>Perfil</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td><b>{user.display_name}</b></td><td>{user.username}</td><td>{roles.find(item=>item.id===user.role)?.name}</td><td><span className={`badge ${user.active?'success':'muted'}`}>{user.active?'Activo':'Inactivo'}</span></td><td><div className="actions"><button onClick={()=>open(user)}>Editar</button><button className={user.active?'danger-text':''} onClick={()=>void toggle(user)}>{user.active?'Desactivar':'Activar'}</button></div></td></tr>)}</tbody></table></div></section>{editing!==undefined&&<Modal title={editing?'Editar usuario':'Nuevo usuario'} onClose={()=>setEditing(undefined)}><form className="form" onSubmit={save}><label className="field">Nombre completo<input required minLength={2} value={name} onChange={e=>setName(e.target.value)}/></label>{!editing&&<label className="field">Usuario<input required minLength={3} value={username} onChange={e=>setUsername(e.target.value)}/></label>}<label className="field">{editing?'Nueva contraseña (opcional)':'Contraseña'}<input required={!editing} minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder={editing?'Dejar vacía para conservarla':'Mínimo 8 caracteres'}/></label><label className="field">Perfil<select value={role} onChange={e=>setRole(e.target.value as UserRole)}>{roles.map(item=><option value={item.id} key={item.id}>{item.name}</option>)}</select></label><div className="role-permission"><KeyRound size={17}/><span>{roles.find(item=>item.id===role)?.description}</span></div><button className="primary full" disabled={saving}>{saving?'Guardando…':'Guardar usuario'}</button></form></Modal>}</>;
}

