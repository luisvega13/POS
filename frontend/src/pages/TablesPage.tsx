import {ArrowLeft,ArrowRightLeft,Ban,Clock3,Eye,Layers3,Minus,Plus,Printer,Search,Trash2,UtensilsCrossed,UsersRound} from 'lucide-react';
import {useEffect,useState} from 'react';
import {api} from '../api';
import {Modal} from '../components/Modal';
import {ValueModeToggle,type ValueMode} from '../components/ValueModeToggle';
import type {CartLine,DiningTable,DiningTableItem,PaymentMethod,Permissions,Product,TableCancellation,User,UserRole} from '../types';
const mxn=(value:number|string)=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'}).format(Number(value));
const dateTime=(value:string)=>new Date(value).toLocaleString('es-MX',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});
const tableName=(name:string)=>/^mesa\b/i.test(name)?name:`Mesa ${name}`;
type DetailLine={key:string;item_id:number|null;product_name:string;quantity:number;subtotal:number;added_at:string|null};
const calculated=(base:number,value:string,mode:ValueMode)=>Math.round((mode==='percent'?base*Number(value||0)/100:Number(value||0))*100)/100;
function detailLines(items:DiningTableItem[],grouped:boolean):DetailLine[]{if(!grouped)return items.map(item=>({key:String(item.id),item_id:item.id,product_name:item.product_name,quantity:item.quantity,subtotal:Number(item.subtotal),added_at:item.added_at}));const combined=new Map<string,DetailLine>();for(const item of items){const key=`${item.product_id}:${item.unit_price}`,current=combined.get(key);current?(current.quantity+=item.quantity,current.subtotal+=Number(item.subtotal)):combined.set(key,{key,item_id:null,product_name:item.product_name,quantity:item.quantity,subtotal:Number(item.subtotal),added_at:null})}return [...combined.values()]}
export function TablesPage({products,cashOpen,role,permissions,onReload,onNotice}:{products:Product[];cashOpen:boolean;role:UserRole;permissions:Permissions;onReload:()=>void;onNotice:(message:string,error?:boolean)=>void}){
 const [tables,setTables]=useState<DiningTable[]>([]),[selected,setSelected]=useState<DiningTable|null>(null),[draft,setDraft]=useState<Map<number,CartLine>>(new Map()),[creating,setCreating]=useState(false),[details,setDetails]=useState(false),[cancellations,setCancellations]=useState<TableCancellation[]>([]),[grouped,setGrouped]=useState(false),[transferring,setTransferring]=useState(false),[targetId,setTargetId]=useState(''),[transferQty,setTransferQty]=useState<Record<number,number>>({}),[cancelItem,setCancelItem]=useState<DiningTableItem|null>(null),[cancelQty,setCancelQty]=useState(1),[cancelReason,setCancelReason]=useState(''),[name,setName]=useState(''),[query,setQuery]=useState(''),[category,setCategory]=useState(''),[paying,setPaying]=useState(false),[method,setMethod]=useState<PaymentMethod>('cash'),[received,setReceived]=useState(''),[tip,setTip]=useState(''),[tipMode,setTipMode]=useState<ValueMode>('amount'),[tipMethod,setTipMethod]=useState<PaymentMethod>('cash'),[discount,setDiscount]=useState(''),[discountMode,setDiscountMode]=useState<ValueMode>('amount'),[discountReason,setDiscountReason]=useState(''),[saving,setSaving]=useState(false),[guestCount,setGuestCount]=useState(1),[waiters,setWaiters]=useState<User[]>([]),[assigning,setAssigning]=useState(false),[waiterId,setWaiterId]=useState(''),[openingWaiterId,setOpeningWaiterId]=useState('');
 const load=async()=>{try{const data=await api.tables();setTables(data);if(selected)setSelected(data.find(table=>table.id===selected.id)??null)}catch(error){onNotice((error as Error).message,true)}};useEffect(()=>{void load();void api.waiters().then(setWaiters).catch(error=>onNotice((error as Error).message,true))},[]);
 const categories=[...new Set(products.map(product=>product.category))],filtered=products.filter(product=>(!category||product.category===category)&&product.name.toLowerCase().includes(query.toLowerCase())),draftLines=[...draft.values()],draftTotal=draftLines.reduce((sum,line)=>sum+Number(line.product.price)*line.quantity,0),tableTotal=Number(selected?.total||0),discountValue=calculated(tableTotal,discount,discountMode),discountedTotal=Math.max(0,tableTotal-discountValue),serviceCharge=permissions.include_tip_in_ticket?Math.round(discountedTotal*permissions.service_charge_percent)/100:0,tipValue=permissions.include_tip_in_ticket?serviceCharge:calculated(discountedTotal,tip,tipMode),cashDue=(method==='cash'?discountedTotal:0)+(tipValue>0&&tipMethod==='cash'?tipValue:0);
 const canPrint=role!=='waiter'||permissions.waiter_print_account,transferMode=role==='admin'?'allowed':permissions[`${role}_transfer_mode` as 'waiter_transfer_mode'|'cashier_transfer_mode'],cancelMode=role==='admin'?'allowed':permissions[`${role}_cancel_mode` as 'waiter_cancel_mode'|'cashier_cancel_mode'];
 async function create(){if(!name.trim()||(role!=='waiter'&&!openingWaiterId))return;setSaving(true);try{const table=await api.createTable(name,permissions.waiter_require_guest_count?guestCount:0,role==='waiter'?undefined:Number(openingWaiterId));setTables(old=>[...old,table]);setSelected(table);setName('');setGuestCount(1);setOpeningWaiterId('');setCreating(false);onNotice(`${table.name} abierta para ${table.assigned_waiter_name}`)}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 function setQty(product:Product,delta:number){setDraft(current=>{const next=new Map(current),quantity=(next.get(product.id)?.quantity??0)+delta;quantity>0?next.set(product.id,{product,quantity}):next.delete(product.id);return next})}
 async function addToTable(){if(!selected||!draftLines.length)return;setSaving(true);try{const saved=await api.addTableItems(selected.id,draftLines.map(line=>({product_id:line.product.id,quantity:line.quantity})));setSelected(saved);setTables(old=>old.map(table=>table.id===saved.id?saved:table));setDraft(new Map());onNotice('Productos agregados a la mesa')}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 async function printAccount(){if(!selected)return;setSaving(true);try{onNotice((await api.printTableAccount(selected.id)).message)}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 async function openDetails(){if(!selected)return;setGrouped(false);setCancellations(await api.tableCancellations(selected.id));setDetails(true)}
 function operationPassword(operation:'cancel'|'transfer'){if(role==='admin')return '';const mode=permissions[`${role}_${operation}_mode` as keyof Permissions];if(mode==='password')return window.prompt('Ingresa la contraseña de un administrador para autorizar la operación');return ''}
 async function cancelProduct(){if(!selected||!cancelItem)return;const operation_password=operationPassword('cancel');if(operation_password===null)return;setSaving(true);try{const saved=await api.cancelTableItem(selected.id,cancelItem.id,{quantity:cancelQty,reason:cancelReason,operation_password});setSelected(saved);setTables(old=>old.map(table=>table.id===saved.id?saved:table));setCancellations(await api.tableCancellations(selected.id));setCancelItem(null);setCancelReason('');onNotice('Producto cancelado y registrado')}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 async function transfer(){if(!selected||!targetId)return;const operation_password=operationPassword('transfer');if(operation_password===null)return;const items=Object.entries(transferQty).filter(([,quantity])=>quantity>0).map(([item_id,quantity])=>({item_id:Number(item_id),quantity}));setSaving(true);try{const result=await api.transferTableItems(selected.id,{target_table_id:Number(targetId),items,operation_password});setSelected(result.source);setTransferring(false);setTransferQty({});setTargetId('');await load();onNotice('Productos traspasados correctamente')}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 async function assignWaiter(){if(!selected||!waiterId)return;const operation_password=operationPassword('transfer');if(operation_password===null)return;setSaving(true);try{const saved=await api.assignTableWaiter(selected.id,Number(waiterId),operation_password);setAssigning(false);setWaiterId('');if(role==='waiter'){setSelected(null);await load()}else{setSelected(saved);setTables(old=>old.map(table=>table.id===saved.id?saved:table))}onNotice(`Mesa asignada a ${saved.assigned_waiter_name}`)}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 async function cancelTable(){if(!selected||!window.confirm(`¿Cerrar ${selected.name} sin cobrar?`))return;try{const result=await api.cancelTable(selected.id);setSelected(null);setDraft(new Map());await load();onNotice(result.message)}catch(error){onNotice((error as Error).message,true)}}
 async function checkout(){if(!selected)return;if(draftLines.length)return onNotice('Primero agrega los productos pendientes a la mesa',true);if(discountMode==='percent'&&Number(discount)>100)return onNotice('El porcentaje de descuento no puede superar 100%',true);if(discountValue>tableTotal)return onNotice('El descuento no puede superar el consumo',true);if(discountValue>0&&!discountReason.trim())return onNotice('Indica el motivo del descuento',true);if(cashDue>0&&Number(received)<cashDue)return onNotice('El efectivo recibido es menor al total en efectivo',true);setSaving(true);try{const result=await api.checkoutTable(selected.id,{payment_method:method,amount_received:cashDue>0?received:null,tip_amount:tipValue,tip_method:tipMethod,discount_amount:discountValue,discount_reason:discountReason,print_ticket:permissions.print_on_checkout});setPaying(false);setSelected(null);setReceived('');setTip('');setDiscount('');setDiscountReason('');await load();onReload();onNotice(result.message,result.print_success===false)}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 function leaveTable(){if(draftLines.length&&!window.confirm('Hay productos sin agregar. ¿Salir y descartarlos?'))return;setDraft(new Map());setSelected(null)}
 if(selected)return <div className="table-service"><div className="table-service-head"><div className="table-context"><button className="table-back" onClick={leaveTable}><ArrowLeft size={17}/> Mesas</button><span className="table-context-divider"/><div className="table-identity"><h2>{tableName(selected.name)}</h2><span><i/> Abierta{selected.guest_count>0?` · ${selected.guest_count} personas`:``}{selected.assigned_waiter_name?` · ${selected.assigned_waiter_name}`:``}</span></div></div><div className="table-head-actions">{waiters.length>0&&transferMode!=='denied'&&<button className="table-detail-button" onClick={()=>{setWaiterId(String(selected.assigned_waiter_id??''));setAssigning(true)}}><UsersRound size={15}/> {selected.assigned_waiter_id?'Traspasar mesa':'Asignar mesero'}</button>}{canPrint&&<button className="table-detail-button" disabled={!selected.items.length||saving} onClick={()=>void printAccount()}><Printer size={15}/> Cuenta</button>}{transferMode!=='denied'&&<button className="table-detail-button" disabled={!selected.items.length||tables.length<2} onClick={()=>setTransferring(true)}><ArrowRightLeft size={15}/> Traspasar</button>}<button className="table-detail-button" onClick={()=>void openDetails()}><Eye size={15}/> Detalle</button>{role!=='waiter'&&<button className="table-close-button" onClick={()=>void cancelTable()}>Cerrar</button>}</div></div><div className="pos-grid"><section className="catalog panel"><div className="search"><Search size={18}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Buscar producto…"/></div><div className="chips"><button className={!category?'active':''} onClick={()=>setCategory('')}>Todos</button>{categories.map(item=><button key={item} className={category===item?'active':''} onClick={()=>setCategory(item)}>{item}</button>)}</div><div className="product-grid">{filtered.map(product=><button className="product-card" key={product.id} onClick={()=>setQty(product,1)}><small>{product.category}</small><b>{product.name}</b><strong>{mxn(product.price)}</strong></button>)}</div></section><aside className="cart panel"><header><div><small>POR AÑADIR</small><h2>Consumo actual</h2></div><button className="text-button" onClick={()=>setDraft(new Map())}>Limpiar</button></header><div className="cart-lines">{draftLines.map(line=><article key={line.product.id}><div><b>{line.product.name}</b><small>{mxn(line.product.price)} c/u</small></div><strong>{mxn(Number(line.product.price)*line.quantity)}</strong><footer><button onClick={()=>setQty(line.product,-1)}><Minus size={15}/></button><span>{line.quantity}</span><button onClick={()=>setQty(line.product,1)}><Plus size={15}/></button><button className="remove" onClick={()=>setQty(line.product,-line.quantity)}><Trash2 size={15}/> Quitar</button></footer></article>)}{!draftLines.length&&<div className="empty"><UtensilsCrossed/><p>Selecciona los productos que deseas añadir</p></div>}</div><div className="cart-total"><p><span>Productos por añadir</span><b>{mxn(draftTotal)}</b></p><button className="primary full" disabled={!draftLines.length||saving} onClick={()=>void addToTable()}>{saving?'Agregando…':'Añadir a la mesa'}</button><p className="saved-total"><span>Total guardado en mesa</span><b>{mxn(selected.total)}</b></p>{role!=='waiter'&&<button className="secondary full" disabled={!selected.items.length||saving} onClick={()=>cashOpen?setPaying(true):onNotice('Debes abrir caja antes de cobrar',true)}>Cobrar mesa</button>}</div></aside></div>
 {details&&<Modal title={`Detalle de ${selected.name}`} onClose={()=>setDetails(false)}><div className="table-detail-summary"><span>Total acumulado</span><strong>{mxn(selected.total)}</strong></div><div className="table-detail-section-head"><div><small>PRODUCTOS AGREGADOS</small><span>{grouped?'Vista resumida':'Ordenados por incorporación'}</span></div></div><div className="table-detail-actions"><button className="detail-group-button" onClick={()=>setGrouped(value=>!value)}><Layers3 size={14}/>{grouped?'Ver historial':'Agrupar'}</button></div><div className="table-detail-list">{detailLines(selected.items,grouped).map(item=><div className="table-detail-row" key={item.key}><span className="detail-quantity">{item.quantity}</span><article><div><b>{item.product_name}</b>{item.added_at&&<small>{dateTime(item.added_at)}</small>}</div><strong>{mxn(item.subtotal)}</strong>{item.item_id&&cancelMode!=='denied'&&<button className="line-cancel" onClick={()=>{setCancelItem(selected.items.find(row=>row.id===item.item_id)??null);setCancelQty(1)}}><Ban size={13}/> Cancelar</button>}</article></div>)}{!selected.items.length&&<div className="empty">Aún no se han agregado productos.</div>}</div>{cancellations.length>0&&<div className="cancellation-history"><small>CANCELACIONES</small>{cancellations.map(item=><article key={item.id}><div><b>{item.quantity} {item.product_name}</b><span>{item.reason}</span></div><div><strong>-{mxn(item.amount)}</strong><small>{item.cancelled_by} · {dateTime(item.cancelled_at)}</small></div></article>)}</div>}</Modal>}
 {cancelItem&&<Modal title="Cancelar producto" onClose={()=>setCancelItem(null)}><div className="operation-product"><b>{cancelItem.product_name}</b><span>{cancelItem.quantity} disponibles</span></div><label className="field">Cantidad<input type="number" min="1" max={cancelItem.quantity} value={cancelQty} onChange={event=>setCancelQty(Number(event.target.value))}/></label><label className="field">Motivo<textarea required value={cancelReason} onChange={event=>setCancelReason(event.target.value)} placeholder="Ej. Error de captura, cliente cambió de opinión…"/></label><button className="danger full" disabled={!cancelReason.trim()||saving} onClick={()=>void cancelProduct()}>Confirmar cancelación</button></Modal>}
 {transferring&&<Modal title="Traspasar productos" onClose={()=>setTransferring(false)}><label className="field">Mesa destino<select value={targetId} onChange={event=>setTargetId(event.target.value)}><option value="">Seleccionar mesa…</option>{tables.filter(table=>table.id!==selected.id).map(table=><option key={table.id} value={table.id}>{tableName(table.name)}</option>)}</select></label><div className="transfer-list">{selected.items.map(item=><label key={item.id}><span><b>{item.product_name}</b><small>Disponible: {item.quantity}</small></span><input type="number" min="0" max={item.quantity} value={transferQty[item.id]??0} onChange={event=>setTransferQty(current=>({...current,[item.id]:Number(event.target.value)}))}/></label>)}</div><button className="primary full" disabled={!targetId||!Object.values(transferQty).some(value=>value>0)||saving} onClick={()=>void transfer()}>Confirmar traspaso</button></Modal>}
 {assigning&&<Modal title="Asignar mesa a mesero" onClose={()=>setAssigning(false)}><div className="transfer-waiter-form" style={{display:'grid',gap:20}}><label className="field">Mesero responsable<select value={waiterId} onChange={event=>setWaiterId(event.target.value)}><option value="">Seleccionar mesero…</option>{waiters.map(waiter=><option key={waiter.id} value={waiter.id}>{waiter.display_name}</option>)}</select></label><button className="primary full" disabled={!waiterId||saving} onClick={()=>void assignWaiter()}>{saving?'Guardando…':'Confirmar traspaso de mesa'}</button></div></Modal>}{paying&&<Modal title={`Cobrar ${selected.name}`} onClose={()=>setPaying(false)}><div className="payment-total"><span>Total a cobrar</span><strong>{mxn(discountedTotal+tipValue)}</strong></div><div className="payment-tabs">{([['cash','Efectivo'],['card','Tarjeta'],['transfer','Transferencia']] as [PaymentMethod,string][]).map(([id,label])=><button key={id} className={method===id?'active':''} onClick={()=>setMethod(id)}>{label}</button>)}</div><div className="payment-adjustments"><label className="field adjustment-field"><span className="field-heading"><span>Descuento</span><ValueModeToggle mode={discountMode} onChange={setDiscountMode} label="Descuento"/></span><span className="amount-control"><i>{discountMode==='amount'?'$':'%'}</i><input type="number" min="0" max={discountMode==='percent'?100:tableTotal} step="0.01" value={discount} onChange={event=>setDiscount(event.target.value)} placeholder="0.00"/></span><small>Aplicado: {mxn(discountValue)}</small></label><label className="field adjustment-field"><span className="field-heading"><span>Propina</span>{!permissions.include_tip_in_ticket&&<ValueModeToggle mode={tipMode} onChange={setTipMode} label="Propina"/>}</span>{permissions.include_tip_in_ticket?<div className="service-charge-summary compact"><span>Servicio {permissions.service_charge_percent}%</span><b>{mxn(serviceCharge)}</b></div>:<><span className="amount-control"><i>{tipMode==='amount'?'$':'%'}</i><input inputMode="decimal" type="number" min="0" step="0.01" value={tip} onChange={event=>setTip(event.target.value)} placeholder="0.00"/></span><small>Calculada: {mxn(tipValue)}</small></>}</label><label className="field">Motivo del descuento<input value={discountReason} onChange={event=>setDiscountReason(event.target.value)} placeholder="Ej. Promoción o cortesía"/></label><label className="field">Método de propina<select value={tipMethod} onChange={event=>setTipMethod(event.target.value as PaymentMethod)}><option value="cash">Efectivo</option><option value="card">Tarjeta</option><option value="transfer">Transferencia</option></select></label></div>{cashDue>0&&<label className="field received-field">Cantidad recibida<input inputMode="decimal" value={received} onChange={event=>setReceived(event.target.value)} placeholder="0.00"/><span>Cambio: <b>{mxn(Math.max(0,Number(received||0)-cashDue))}</b></span></label>}<button className="primary full" disabled={saving} onClick={()=>void checkout()}>{saving?'Registrando…':permissions.print_on_checkout?'Confirmar cobro e imprimir':'Confirmar cobro'}</button></Modal>}</div>;
 return <><div className="section-title"><div><h2>Mesas abiertas</h2><p>Conserva varios consumos y cobra cuando el cliente lo solicite.</p></div><button className="primary" onClick={()=>setCreating(true)}><Plus size={17}/> Abrir mesa</button></div><section className="tables-grid">{tables.map(table=><button className="table-card panel" key={table.id} onClick={()=>{setSelected(table);setDraft(new Map())}}><span className="table-icon"><UtensilsCrossed size={20}/></span><div><small>EN SERVICIO</small><h3>{table.name}</h3><p><Clock3 size={14}/>{new Date(table.opened_at).toLocaleTimeString('es-MX',{hour:'2-digit',minute:'2-digit'})} · {table.items.reduce((sum,item)=>sum+item.quantity,0)} productos{table.guest_count>0?` · ${table.guest_count} personas`:``}{table.assigned_waiter_name?` · ${table.assigned_waiter_name}`:``}</p></div><strong>{mxn(table.total)}</strong></button>)}{!tables.length&&<div className="empty-state panel"><UtensilsCrossed size={38}/><h3>No hay mesas abiertas</h3><p className="muted-copy">Abre una mesa para comenzar a registrar su consumo.</p></div>}</section>{creating&&<Modal title="Abrir una mesa" onClose={()=>setCreating(false)}><label className="field">Nombre o número de mesa<input autoFocus value={name} onChange={event=>setName(event.target.value)} onKeyDown={event=>event.key==='Enter'&&void create()} placeholder="Ej. Mesa 4, Terraza 2…"/></label>{role!=='waiter'&&<label className="field">Mesero responsable<select value={openingWaiterId} onChange={event=>setOpeningWaiterId(event.target.value)}><option value="">Seleccionar mesero…</option>{waiters.map(waiter=><option key={waiter.id} value={waiter.id}>{waiter.display_name}</option>)}</select></label>}{permissions.waiter_require_guest_count&&<label className="field">Número de personas<input type="number" min="1" max="100" value={guestCount} onChange={event=>setGuestCount(Number(event.target.value))}/></label>}<button className="primary full" disabled={!name.trim()||saving||(role!=='waiter'&&!openingWaiterId)||(permissions.waiter_require_guest_count&&guestCount<1)} onClick={()=>void create()}>{saving?'Abriendo…':'Abrir mesa'}</button></Modal>}</>;
}


