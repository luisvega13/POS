import {Minus,Plus,Search,ShoppingBag,Trash2} from 'lucide-react';
import {useState} from 'react';
import {api} from '../api';
import type {CartLine,PaymentMethod,Permissions,Product} from '../types';
import {Modal} from '../components/Modal';
import {ValueModeToggle,type ValueMode} from '../components/ValueModeToggle';
const mxn=(value:number|string)=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'}).format(Number(value));
const calculated=(base:number,value:string,mode:ValueMode)=>Math.round((mode==='percent'?base*Number(value||0)/100:Number(value||0))*100)/100;
export function PosPage({products,cashOpen,permissions,onReload,onNotice}:{products:Product[];cashOpen:boolean;permissions:Permissions;onReload:()=>void;onNotice:(message:string,error?:boolean)=>void}){
 const [query,setQuery]=useState(''),[category,setCategory]=useState(''),[cart,setCart]=useState<Map<number,CartLine>>(new Map()),[paying,setPaying]=useState(false),[method,setMethod]=useState<PaymentMethod>('cash'),[received,setReceived]=useState(''),[tip,setTip]=useState(''),[tipMode,setTipMode]=useState<ValueMode>('amount'),[tipMethod,setTipMethod]=useState<PaymentMethod>('cash'),[discount,setDiscount]=useState(''),[discountMode,setDiscountMode]=useState<ValueMode>('amount'),[discountReason,setDiscountReason]=useState(''),[saving,setSaving]=useState(false);
 const categories=[...new Set(products.map(product=>product.category))],filtered=products.filter(product=>(!category||product.category===category)&&product.name.toLowerCase().includes(query.toLowerCase())),lines=[...cart.values()],total=lines.reduce((sum,line)=>sum+Number(line.product.price)*line.quantity,0),discountValue=calculated(total,discount,discountMode),discountedTotal=Math.max(0,total-discountValue),serviceCharge=permissions.include_tip_in_ticket?Math.round(discountedTotal*permissions.service_charge_percent)/100:0,tipValue=permissions.include_tip_in_ticket?serviceCharge:calculated(discountedTotal,tip,tipMode),cashDue=(method==='cash'?discountedTotal:0)+(tipValue>0&&tipMethod==='cash'?tipValue:0);
 const setQty=(product:Product,delta:number)=>setCart(current=>{const next=new Map(current),quantity=(next.get(product.id)?.quantity||0)+delta;quantity>0?next.set(product.id,{product,quantity}):next.delete(product.id);return next});
 async function confirm(){if(discountMode==='percent'&&Number(discount)>100)return onNotice('El porcentaje de descuento no puede superar 100%',true);if(discountValue>total)return onNotice('El descuento no puede superar el consumo',true);if(discountValue>0&&!discountReason.trim())return onNotice('Indica el motivo del descuento',true);if(cashDue>0&&Number(received)<cashDue)return onNotice('El efectivo recibido es menor al total en efectivo',true);setSaving(true);try{const result=await api.createSale({items:lines.map(line=>({product_id:line.product.id,quantity:line.quantity})),payment_method:method,amount_received:cashDue>0?received:null,tip_amount:tipValue,tip_method:tipMethod,discount_amount:discountValue,discount_reason:discountReason,print_ticket:true});setCart(new Map());setPaying(false);setTip('');setReceived('');setDiscount('');setDiscountReason('');onNotice(result.message,result.print_success===false);onReload()}catch(error){onNotice((error as Error).message,true)}finally{setSaving(false)}}
 return <div className="pos-grid"><section className="catalog panel"><div className="search"><Search size={18}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Buscar producto…"/></div><div className="chips"><button className={!category?'active':''} onClick={()=>setCategory('')}>Todos</button>{categories.map(item=><button key={item} className={category===item?'active':''} onClick={()=>setCategory(item)}>{item}</button>)}</div><div className="product-grid">{filtered.map(product=><button className="product-card" key={product.id} onClick={()=>setQty(product,1)}><small>{product.category}</small><b>{product.name}</b><strong>{mxn(product.price)}</strong></button>)}{!filtered.length&&<div className="empty">No hay productos que coincidan.</div>}</div></section><aside className="cart panel"><header><div><small>ORDEN ACTUAL</small><h2>Venta</h2></div><button className="text-button" onClick={()=>setCart(new Map())}>Limpiar</button></header><div className="cart-lines">{lines.map(line=><article key={line.product.id}><div><b>{line.product.name}</b><small>{mxn(line.product.price)} c/u</small></div><strong>{mxn(Number(line.product.price)*line.quantity)}</strong><footer><button onClick={()=>setQty(line.product,-1)}><Minus size={15}/></button><span>{line.quantity}</span><button onClick={()=>setQty(line.product,1)}><Plus size={15}/></button><button className="remove" onClick={()=>setQty(line.product,-line.quantity)}><Trash2 size={15}/> Quitar</button></footer></article>)}{!lines.length&&<div className="empty"><ShoppingBag/><p>Agrega productos para comenzar</p></div>}</div><div className="cart-total"><p><span>Subtotal</span><b>{mxn(total)}</b></p><p className="grand"><span>Total</span><b>{mxn(total)}</b></p><button className="primary full" disabled={!lines.length} onClick={()=>cashOpen?setPaying(true):onNotice('Debes abrir caja antes de vender',true)}>Cobrar</button></div></aside>{paying&&<Modal title="Cobrar venta" onClose={()=>setPaying(false)}><div className="payment-total"><span>Total a cobrar</span><strong>{mxn(discountedTotal+tipValue)}</strong></div><div className="payment-tabs">{([['cash','Efectivo'],['card','Tarjeta'],['transfer','Transferencia']] as [PaymentMethod,string][]).map(([id,label])=><button key={id} className={method===id?'active':''} onClick={()=>setMethod(id)}>{label}</button>)}</div><div className="payment-adjustments"><label className="field adjustment-field"><span className="field-heading"><span>Descuento</span><ValueModeToggle mode={discountMode} onChange={setDiscountMode} label="Descuento"/></span><span className="amount-control"><i>{discountMode==='amount'?'$':'%'}</i><input type="number" min="0" max={discountMode==='percent'?100:total} step="0.01" value={discount} onChange={event=>setDiscount(event.target.value)} placeholder="0.00"/></span><small>Aplicado: {mxn(discountValue)}</small></label><label className="field adjustment-field"><span className="field-heading"><span>Propina</span>{!permissions.include_tip_in_ticket&&<ValueModeToggle mode={tipMode} onChange={setTipMode} label="Propina"/>}</span>{permissions.include_tip_in_ticket?<div className="service-charge-summary compact"><span>Servicio {permissions.service_charge_percent}%</span><b>{mxn(serviceCharge)}</b></div>:<><span className="amount-control"><i>{tipMode==='amount'?'$':'%'}</i><input inputMode="decimal" type="number" min="0" step="0.01" value={tip} onChange={event=>setTip(event.target.value)} placeholder="0.00"/></span><small>Calculada: {mxn(tipValue)}</small></>}</label><label className="field">Motivo del descuento<input value={discountReason} onChange={event=>setDiscountReason(event.target.value)} placeholder="Ej. Promoción o cortesía"/></label><label className="field">Método de propina<select value={tipMethod} onChange={event=>setTipMethod(event.target.value as PaymentMethod)}><option value="cash">Efectivo</option><option value="card">Tarjeta</option><option value="transfer">Transferencia</option></select></label></div>{cashDue>0&&<label className="field received-field">Cantidad recibida<input inputMode="decimal" value={received} onChange={event=>setReceived(event.target.value)} placeholder="0.00"/><span>Cambio: <b>{mxn(Math.max(0,Number(received||0)-cashDue))}</b></span></label>}<button className="primary full" disabled={saving} onClick={()=>void confirm()}>{saving?'Registrando…':'Confirmar cobro'}</button></Modal>}</div>;
}
